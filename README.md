# UX Audit Screenshots — 2026-07-17

Screenshot corpus for the full-app UX/UI audit (task 01KXR2G078WK4DSCS2NQ5D5PKE).
Referenced inline by the audit report in the companion PR
(branch `sam/next-5-hours-thoroughly-5d5pke`,
`tasks/active/2026-07-17-full-app-ux-audit.md`).

- `before/` — 615 captures of the app as it exists on `main` (d6b3d08db):
  the full Playwright audit corpus (78 specs) at mobile 375px + desktop 1280px,
  dark + light, plus the new uncovered-pages sweep.
- `after/` — 191 re-captures of every surface changed by the audit fixes
  (F1–F12), same viewports/themes.

Same filename in both directories = same surface/state, before vs after.
This branch is an artifact store — do not merge it.
