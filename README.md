# PR screenshot assets — chat session tool rail

Screenshot evidence for PR "Surface session controls in a chat tool rail".

This is an **orphan branch that is never merged**. It exists only to host public,
stable image URLs for the PR body, so the screenshots do not land in the merge diff.
Delete it once the PR is merged and the evidence no longer needs to render.
