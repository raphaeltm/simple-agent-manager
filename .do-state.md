# /do Workflow State

## Task

Fix scheduler sleep leaks and node-pool migration gaps after Fable approves the implementation plan.

## Task File

tasks/active/2026-08-31-fix-scheduler-sleep-leaks-node-pools.md

## Branch

sam/fix-scheduler-sleep-node-pools

## Worktree

/workspaces/sam-scheduler-sleep-node-pools

## Current Phase

Phase 4: Pre-PR Validation

## Phase Checklist

- [x] Phase 0: Fable Plan Approval Gate
- [x] Phase 1: Research & Task Creation
- [x] Phase 2: Worktree Setup
- [x] Phase 3: Implementation
- [x] Phase 4: Pre-PR Validation
- [ ] Phase 5: Review
- [ ] Phase 6: Staging Verification
- [ ] Phase 7: Pull Request & Post-Merge Deploy Monitoring

## Fable Approval Gate

- Original Fable task: `01M1CG1ZNYKS2FX53JBMS5Y6E0` — FAILED before starting; queued 1385s at node selection.
- Completed Fable retry task: `01M1CHF939QC5DQZEC5DAHYBFW` — returned `CHANGES REQUIRED`.
- Fable retry title: `Review Fable scheduler session-lifecycle and compute-pool remediation plan`
- Completed Fable re-review task: `01M1CK9NX0FQBM96X2VWG8E4HH` — returned `APPROVED`.
- Required verdict before implementation: `APPROVED`
- Blocking verdict: `CHANGES REQUIRED`
- Durable wait key: `scheduler-node-pool-fable-review-v3`
- Fable v2 verified no files edited, no commits, no PR, no deployments.
- Fable v3 dispatch call timed out after 300s, but `list_tasks` and `search_messages` confirmed task `01M1CK9NX0FQBM96X2VWG8E4HH` exists and is in_progress.
- Fable v3 output summary: `APPROVED`; implementation may proceed under the gate.

## Phase 5: Review Tracker

- First task-completion review: `FAIL`.
  - Missing bounded repair for stale in-flight sleep rows.
  - Missing deterministic stranded `stopping` convergence.
  - Missing explicit production sweep ordering test.
  - Writer inventory/disposition incomplete.
- First test-engineer review: coverage gaps for in-flight predicate, terminal-node repair, provider metadata vertical coverage, and API/UI metadata rendering.
- First Cloudflare review: terminal-node repair could terminalize protected in-flight sleep workspaces before sleep repair/sweep; repair query needed bounded operational behavior.
- Remediation in progress: added `session_sleep_lifecycle_repair`, moved it before node cleanup, made terminal-node repair skip protected sleep rows, added focused tests.
- Second rereview agents were interrupted after several minutes because they were contending on heavyweight local validation and returned no findings.

## Implementation Progress

- [x] Prevent terminal-ledger reconciliation from racing in-flight sleep lifecycle transitions.
- [x] Enumerate every `stopSession`/`failSession` writer and give each a per-writer disposition in the task/PR.
- [x] Extract one shared restorable-or-in-flight sleep predicate and apply it to all three sleeping-only destroyer guards: terminal reconciliation, session-summary ledger reconciliation, and workspace lifecycle finalizer/snapshot recovery.
- [x] Add an env-configurable absolute ceiling plus escape path for wedged in-flight sleeps so `stopping` cannot defer forever.
- [x] Add narrowly guarded repair for already-stranded terminal sessions/snapshots.
- [x] Repair stale workspace, agent-session, and open compute-usage ledger rows when nodes are terminal/deleted.
- [x] Route session recovery through the centralized effective capacity-pool resolver exactly once, feeding both recovery-task snapshot columns and TaskRunner config while preserving supersession CAS.
- [x] Distinguish positively invalid pool configuration from transient resolver errors: invalid pool fails wake visibly; transient errors defer without consuming recovery attempts.
- [x] Expose concrete provider-native offering metadata through API/shared/UI while labeling legacy size as a compatibility hint.
- [x] Use provider instance resource metadata for compute and node usage accounting with legacy fallback.
- [x] Determine deployment-node pool scope from existing architecture and implement or document accordingly.
- [ ] Add focused tests, docs, validation, local specialist reviews, staging verification, PR, CI, CodeRabbit loop, merge, and production deploy monitoring.

## Notes

- User explicitly required validation by `@Fable chat` before implementation, then one `/do` implementation/PR.
- Production evidence points to a scheduler ordering bug: terminal cleanup queues sleep, terminal ledger reconciliation stops the ProjectData chat before the sleep sweep can transition it, leaving snapshots stuck in `stopping`.
- Node-pool migration gaps include legacy `small`/`medium` exposure, vCPU metering from legacy size, and recovery paths bypassing capacity-pool selection.
- Fable v2 blockers to incorporate:
  1. Fix all three sleeping-only destroyer guards using one shared predicate, not just terminal-session reconciliation.
  2. Add an absolute ceiling and escape path for wedged in-flight sleeps to avoid unbounded `stopping` deferral.
  3. Recovery re-resolution must go through the shared placement resolver once, feed snapshot columns and TaskRunner config, recompute credential attribution coherently, and leave supersession CAS untouched.
  4. Distinguish positively invalid pools from transient resolver errors.
- Non-blocking refinements: reuse existing stopping-claim machinery for repair, add metering billable-entity overlap tests, keep backfills bounded, preserve UI screenshot and staging cleanup obligations, and document deployment-node pool scope.
- Fable v3 refinements to pin in the task file: disposition predicate copies including `task-runtime-liveness.ts`; keep shared predicate scope-keyed and SQL-capable for summary-ledger CAS; include retry-eligible `failed` snapshots in in-flight defer set; ceiling uses per-cycle anchor that resets on success and routes restorable wedges to repair; placement resolution happens before recovery-attempt-consuming claim; restore exact production sweep-ordering regression test; snapshot provider identity/vCPU onto `compute_usage` at start rather than joining deleted nodes.
- Phase 1 task file created at `tasks/backlog/2026-08-31-fix-scheduler-sleep-leaks-node-pools.md`.
- Phase 2 worktree created at `/workspaces/sam-scheduler-sleep-node-pools`; feature branch pushed.
- Task file moved to `tasks/active/2026-08-31-fix-scheduler-sleep-leaks-node-pools.md` in commit `c61bcd03d`.
- Phase 2 baseline passed: `pnpm install`, `pnpm typecheck`, and `pnpm lint` completed. Lint reported existing warnings only in acp-client/web.
- Implemented shared SQL-capable sleep lifecycle predicate in `session-snapshot-sleep-predicate.ts`, wired terminal reconciliation, session-summary reconciliation, and workspace lifecycle finalizer. API typecheck passed for this slice.
- Routed VM session recovery through centralized placement resolution before snapshot recovery claim. Recovery now snapshots resolved capacity-pool metadata into the recovery task, passes the same `capacityPoolSelection` into TaskRunner, preserves source-task CAS, returns visible invalid-placement/credential errors, and defers transient resolver failures without consuming `recovery_attempts`. API typecheck passed for this slice.
- Added bounded `terminal_node_lifecycle_repair` cron sweep plus finalizer workspace-status repair. It marks active-looking workspaces on terminal nodes as stopped, closes non-terminal `agent_sessions`, closes open `compute_usage`, and runs the existing guarded ProjectData cleanup path without touching sleeping workspaces or replaying work. API typecheck passed for this slice.
- Added D1 migration `0132_compute_usage_provider_instance_metadata.sql`, shared/API fields for provider instance metadata, compute/node usage accounting that prefers provider vCPU counts with legacy fallback, and UI labels that show provider-native instance identity/resources before legacy `small`/`medium`/`large` compatibility hints. API + web typechecks passed for this slice after rebuilding shared declarations.
- Documented deployment nodes as intentionally outside workspace compute-pool resolver scope; they remain governed by deployment placement settings/limits. Added targeted tests for provider-vCPU metering and terminal-node lifecycle repair. Validation passed: shared build, API typecheck, web typecheck, and 73 focused API tests.
- Phase 4 validation passed after import sorting: `pnpm typecheck`, `pnpm lint`, and `pnpm --filter @simple-agent-manager/api test -- tests/unit/services/node-usage.test.ts tests/unit/services/workspace-lifecycle-finalizer.test.ts tests/integration/compute-usage.test.ts tests/integration/session-recovery-handoff.test.ts`. Lint still reports the known baseline warnings in `packages/acp-client` and `apps/web`, with zero errors.
- Local review round 1 found a real lifecycle gap: the shared predicate preserved bounded in-flight sleep rows, but stale post-capture wedges had no repair path before terminal-node cleanup. Added bounded `session_sleep_lifecycle_repair`, ordered it before node cleanup, made terminal-node repair skip protected sleep rows, and added focused tests. Focused remediation validation passed: shared build, API typecheck, web typecheck, and 77 focused API tests.
