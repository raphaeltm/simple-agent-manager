# Cross-Boundary State With Multiple Consumers Must Be Reconciled, Not Just Reported

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `apps/api/.claude/rules/57-write-only-cross-boundary-state.md`
