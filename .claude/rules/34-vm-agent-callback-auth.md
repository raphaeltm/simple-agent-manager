# VM Agent Callback Routes Must Use Callback JWT Auth (ABSOLUTE RULE)

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `apps/api/.claude/rules/34-vm-agent-callback-auth.md`
- `packages/vm-agent/.claude/rules/34-vm-agent-callback-auth.md`
