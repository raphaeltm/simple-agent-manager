# VM Agent Diagnostic Getters Must Synchronize Loop-Mutated Fields

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `packages/vm-agent/.claude/rules/46-vm-agent-diagnostic-getter-sync.md`
