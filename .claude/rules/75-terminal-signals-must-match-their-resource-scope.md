# A Terminal Signal May Only Shut Down State At The Scope Of The Resource It Names

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `packages/vm-agent/.claude/rules/75-terminal-signals-must-match-their-resource-scope.md`
