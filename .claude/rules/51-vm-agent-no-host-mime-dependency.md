# VM Agent Behavior Must Not Depend on Host-Provided MIME/OS Databases

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `packages/vm-agent/.claude/rules/51-vm-agent-no-host-mime-dependency.md`
