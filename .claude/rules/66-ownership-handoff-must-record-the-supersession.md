# An Ownership Handoff Must Record That It Happened

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `apps/api/.claude/rules/66-ownership-handoff-must-record-the-supersession.md`
