# A Code Path That Only Activates Under an Emergency Configuration Needs Tests In That Configuration

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `apps/api/.claude/rules/69-emergency-config-paths-need-their-own-coverage.md`
