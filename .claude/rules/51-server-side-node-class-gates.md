# Server-Side Node-Class / Identity Gates — Never Trust a Client-Supplied Identifier or Agent Abstention

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `apps/api/.claude/rules/51-server-side-node-class-gates.md`
