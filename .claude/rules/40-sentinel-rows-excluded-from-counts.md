# Internal/Sentinel Rows Must Be Excluded From Business-Logic Counts

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `apps/api/.claude/rules/40-sentinel-rows-excluded-from-counts.md`
