# A New Required Column Can Silently Delete a Capability for Every Existing Row

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `apps/api/.claude/rules/71-tightening-a-column-can-delete-a-capability.md`
