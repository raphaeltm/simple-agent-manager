# Capture Handler Prerequisites Before a Long Async Operation

This full rule is path-scoped to reduce Claude startup context. Load the scoped copy when working in the matching area.

- `apps/api/.claude/rules/49-capture-prerequisites-before-async-completion.md`
