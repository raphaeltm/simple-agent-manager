# Credential event review fixes for the integrated eventing PR

## Scope and constraints

Repair implementation slice C from `a6c764206..b94a1e8dc` before accepting it into the full eventing feature. This is a technical validation record for `2026-09-06-eventing-delivery-scheduling-channels.md`, not a separate product feature or PR. Keep the parent integration branch `sam/look-eventing-system-weve-rrszn5` and its one-PR, no-merge constraint. Child work must not create a PR or deploy to staging. The parent coordinates final validation.

Independent read-only security and Go review found the issues below. Root reproduced the existing unit suite passing (29 tests in three files) and the file-size gate failing (916 lines in `credential-limit-events.ts`). Passing fake-D1 tests did not establish real storage/admission behavior. The C checklist was reopened pending remediation and verification.

## Required fixes and evidence

- [ ] **Credential audience:** Personal credential events currently enter unrestricted project matching. Another project member can subscribe to `sam.credential_limit` and receive utilization, reset time, credential reference and session IDs. Enforce the existing personal-versus-shared resource boundary using server-derived visibility, at matching and every read/delivery boundary. Shared-project credentials remain usable by project members; personal credentials remain user-scoped. Test two distinct users in one project, an unrelated project, and authorized affected-session delivery. Future catalog/history and standing-watch consumers must apply the same restriction; filtering only the first match path is insufficient.
- [ ] **Telemetry origin:** Go recursively searches arbitrary `SessionNotification` data for `_claude/rateLimit`, so tool input/output can forge provider telemetry. The pinned ACP Go SDK supports typed `UsageUpdate`. Read only the actual adapter-owned usage metadata variant. Test real wire decoding through `SessionUpdate` and the authenticated callback, plus malicious tool metadata that produces no observation.
- [ ] **Provider reset units:** Claude's numeric `resetsAt` is epoch seconds, but the new parser casts it directly to milliseconds. Verify the pinned upstream adapter contract and convert at that boundary. Test numeric real-wire fixtures, not only an ISO string the SDK does not emit.
- [ ] **Bounded reporting lifecycle:** The Go callback launches one goroutine per notification, with unordered background-context retries and no final drain in `Stop`. Use a bounded serialized reporter, safe coalescing, cancellation-aware retries and a finite shutdown flush before idle/teardown. Test bounded concurrency, ordering, retry exhaustion, cancellation and final observation delivery. It must not pin compute indefinitely.
- [ ] **Observation time:** Callback times are only checked for nonnegative values. A far-future sample suppresses later legitimate observations; old first samples are admitted, and client-derived `receivedAt` can affect subscription timing. Capture server receipt time, validate safe timestamp range and configurable maximum age/future skew before writes, and retain source time as evidence. Test future poisoning, expired first samples, equal-time conflicts and normal out-of-order arrival.
- [ ] **Unknown remains unknown:** `computeLevel` maps unknown status with no utilization to `ok`, causing a later reset-only or limit-only header to clear a rejection. Do not lower severity or emit reset without affirmative recovery evidence. Test rejected/critical followed by partial and unsupported telemetry, then a real recovery.
- [ ] **Atomic transitions and durable admission:** The current read-window → compute edge → admit-to-DO → upsert-window sequence races concurrent observations and can lose its state checkpoint after admission. Delivery identity uses transition/reset/source while its fingerprint includes changing observation metadata; repeated same-window crossings can conflict. Admission outcomes are ignored. Independent Cloudflare review exercised the actual producer with dynamic boundary stubs: warning → reset → warning in one reset window reports three admissions although the third conflicts; a delayed older reset is emitted after a newer critical observation despite the final D1 CAS; admission followed by D1 failure causes the next warning to conflict while reporting success. Establish an atomic conditional transition plus immutable emission intent, with stable payload and identity across retries, then recover cross-store admission. Handle conflict/capacity outcomes truthfully. Compose with slice B's source-recovery mechanism where appropriate, without a second generic queue. Prove these three failure sequences and first-admission recovery with real D1 plus canonical ProjectData admission; stubs alone are insufficient final evidence.
- [ ] **Bounded input and storage:** Callback `rateLimits` has no array cap, and arbitrary provider/source/window identifiers can create unlimited persistent state even without an event. Allowlist supported adapter identifiers, cap body/array sizes and apply authenticated bounded admission. Add indexed finite retention/reclamation and bounds for both state and retry records, with shared defaults and overrides. Test oversized and unsupported batches, bounded maintenance and production-shaped seek behavior.
- [ ] **Preserve successful inference:** Proxy attribution persistence is awaited after upstream success outside the best-effort boundary. Its D1 exception turns a paid successful request into a 502 and skips token accounting. Either establish required attribution safely before the billable request or isolate advisory telemetry persistence failures without weakening identity checks. Test successful streaming forwarding/accounting when telemetry persistence fails.
- [ ] **Preserve Anthropic error evidence:** The translated Anthropic error response drops rate-limit and `Retry-After` headers before telemetry extraction. Preserve the same allowlisted headers used by other paths. Verify token-window 429 extraction and reset evidence through the actual translated route.
- [ ] **Credential rotation:** Confirmed by executing the actual UPDATE SQL against SQLite: a delayed proxy response for credential A overwrites a newer injected boot credential B, and valid B callbacks then fail with `Credential attribution mismatch`. Proxy observations already retain per-request A; they must not overwrite boot fields used to verify direct ACP callbacks. Keep injected credentials bound to their runtime generation. Test overlapping old/new proxy responses and delayed ACP callbacks. Timestamp ordering alone cannot fix these different identity lifetimes.
- [ ] **Structure and documentation:** Split the 916-line service into cohesive modules; do not add a file-size exemption. Update canonical API/environment/deployment documentation for new supported paths, unknown cases, bounds and configuration. Keep token analytics and cross-account routing outside this feature.

## Validation gate

Run meaningful real-D1/workerd tests for the migration, signed callback authorization, visibility and producer-to-event path. Preserve existing proxy accounting and event tests. Run shared build before dependent checks, API lint/typecheck, file-size quality, and Go tests with the repository-pinned toolchain. Run concurrency/race tests where applicable. Record actual commands and results; distinguish mocks from real runtime evidence. Independent reviewers must re-check the fixes before final whole-PR review and staging. The full parent release scope remains pending.


## Parent review of C2 checkpoint `73ebe5e7e`

This checkpoint is pushed and fetched but not integrated or accepted. Its checked-off child notes and self-review do not replace the required independent and real-storage evidence. Root inspected the exact source; an independent Go reviewer inspected the changed Go code without rerunning Go. The parent still requires all original acceptance criteria.

- [ ] **One recoverable admission path:** `credential-limit-events/admissions.ts` creates a second admission queue. Its only retry caller is the next `recordCredentialLimitObservations` request; no alarm/cron recovery is wired. A failed final observation can remain pending forever once its agent stops. B's generic outbox was exposed on the parent branch before this checkpoint; compose immutable conditional capture and autonomous recovery through that boundary. C2 owns this integration with B2, not an unspecified later parent follow-up. Preserve truthful finite outcomes and immutable envelopes.
- [ ] **Capacity must not consume the edge:** Root executed the checkpoint's actual conditional INSERT and window UPSERT against SQLite with migrations 0145/0146. With the final admission slot filled after preflight, INSERT changes 0 rows but UPSERT changes 1, advancing the window to `warning` and `last_event_delivery_key=lost-key` while no such admission exists. The next equivalent observation is ignored, losing the event. Gate state advancement and emission capture on the same winning proof; test concurrent capacity after preflight with actual D1, plus retry after capacity releases.
- [ ] **Real recovery/concurrency and storage bounds:** Checkpoint has no required real-D1/canonical-DO producer tests. Admission dispatch has no claim generation/lease, checks supersession before an external await, and mutates retry counters without fencing. Prove overlapping retries, old in-flight transition vs newer committed observation, failure after DO acceptance, claim exhaustion and truthful persisted outcomes. Current retention runs only when a new edge arrives; no `DELETE FROM credential_limit_windows` exists. Active-capacity COUNT, due ordering and project expiry predicates require production-shaped query-plan/scan proof and bounded automatic maintenance.
- [ ] **Authoritative audience everywhere:** The checkpoint's `subscriptionCanMatchProjectEvent` checks personal-event target only, not affected user; absent user/target metadata can fail open in the read helper. It also does not integrate the audience rule into A's new automatic wake materialization. Use server-derived durable audience fields and shared fail-closed authorization across matching, materialization/recovery, all reads/acks and future history/watch paths. DO047 remains reserved for C2, coordinate additive changes with A3/DO046. Legacy malformed personal events must not become globally visible. Validate two users plus real automatic delivery, not only hand-constructed visibility helpers.
- [ ] **Preserve independent usage windows:** Go's one `usageReportPending` slot (`session_host_usage.go:301` in the checkpoint) overwrites unrelated credentials/windows. A blocked callback followed by seven-day rejection then five-hour update loses the rejection; the new test explicitly expects a distinct window to disappear. Coalesce only equivalent updates, retain bounded distinct credential/window state and meaningful rejection/reset transitions. Test these actual delivery scenarios.
- [ ] **Drain at actual lifecycle boundaries:** `Suspend()` cancels `h.ctx` without flushing, while the new reporter uses that context; queued/retrying observations are discarded. `Stop()` reports idle before its flush. Share finite draining before Stop/Suspend lifecycle completion and cancellation, without indefinite compute retention. The reporter ignores send failure and flush returns success merely because the worker exited: propagate unsuccessful delivery honestly and bound the total drain/retry budget.
- [ ] **Fence the originating ACP connection:** Usage capture reads mutable host credential state; ACP clients retain only a host pointer and no immutable process/credential generation. A delayed old-connection usage notification can be relabeled with the replacement credential and accepted by API current-reference checks. Fence capture to its originating connection/generation and test delayed old-client `SessionUpdate` after replacement. Existing prepared requests copying attribution does not solve capture-time relabeling.

Root confirms typed `UsageUpdate.Meta` selection and Claude numeric epoch-seconds conversion are improved. Independent Go review still returned CHANGES REQUIRED. Add actual `SessionUpdate` to HTTP delivery tests, Stop/Suspend ordering/cancellation, multiple-window transitions, credential replacement and failed-flush reporting. Do not describe self-checklists as independent review or mark unresolved runtime/storage criteria done.

## Parent review of C2 checkpoint `27564546de`

This checkpoint is fetched but not integrated or accepted. It removes the separate admission table in favor of the source outbox, adds a cron caller for window retention, and derives persisted event audience fields. The conditional window write now requires its newly inserted intent, repairing the earlier capacity-without-intent loss by inspection. Independent Go review confirms Stop/Suspend now call the drain before idle/cancel and ACP clients retain copied connection attribution. These improvements do not close the original acceptance checklist.

- [ ] **Compute transitions against the state actually replaced.** The producer reads the window before asynchronous work, then the non-edge path writes with only an observed-time comparison. Root executed the actual checkpoint producer/admission functions and migration SQL in memory, substituting only external ProjectData admission, logging and generated IDs. Seed `ok@t0`; begin `ok@t2` and pause after its window read; fully admit `critical@t1`; resume `ok@t2`. The latter returns ignored/ok and stores `last_event_level=ok`, while the outbox contains only the critical event: the required reset is lost. Capture and non-edge writes must fence the expected predecessor and recompute on contention; monotonic timestamps alone do not make edge computation atomic. Add both this lost-reset case and concurrent duplicate-threshold crossings through real SQL.
- [ ] **Make global window retention seekable.** `purgeExpiredCredentialLimitWindows` filters/orders by `updated_at`, but 0146 only adds a project-leading updated-time index; the global cron omits project. Root executed the exact DELETE with migrations 0144–0146 against 1,000 and 20,000 healthy rows: 3,020 versus 60,020 SQLite instructions, SCAN plus temporary ORDER BY tree, zero deleted rows. Add an appropriate bounded seek path and prove work remains bounded for an empty due set. The returned-row limit does not bound scans.
- [ ] **Finish composition with B3's current source API.** This checkpoint still derives its source module from pre-B2 code and embeds another canonical-outbox capture implementation in credential admissions. B3 task `01M1X2YGFS32GY8S1X1XK378JR` owns the final typed conditional capture interface, claim fencing, mutation/wall budgets and retention. Coordinate directly; preserve B3's terminal timestamps and ownership tokens on credential supersession paths. Do not overwrite the current parent source implementation with this old branch version. Verify admission/supersession at the external boundary and the final observation's autonomous recovery.
- [ ] **Preserve chronological meaningful Go observations.** The new pending map replaces an existing key in place. For one window/reset, blocked delivery followed by `allowed@1`, `rejected@2`, `allowed@3` sends `allowed@3` before `rejected@2`; the API drops the rejection as stale. The key also omits utilization, so warning/critical crossings within the same provider status can disappear. Compress only observations proven equivalent without reordering meaningful transitions; test the delivered server state/events, not only map contents.
- [ ] **Close telemetry admission before shutdown drain.** Stop/Suspend wait for the currently observed reporter, but enqueue remains open. A late SDK callback can start a new background-context reporter after the drain's idle check or after lifecycle completion. Establish an ingress/handler barrier and close admission before draining; ensure callbacks cannot restart the reporter afterward. Exercise actual Stop, Suspend and ACP attachment rather than constructing only a client with a host pointer.
- [ ] **Report bounded-queue loss honestly.** Capacity eviction drops pending observations without a failed-delivery disposition; the flush can still return nil. Make overflow observable as delivery failure and prove the bounded policy preserves meaningful transitions or reports their loss. Include rejection/reset, overflow, late callback and replacement-during-retry tests.

The Go findings above are independent code-path review, not rerun Go tests. The root lost-reset and retention probes are executed in-memory evidence, not workerd or live provisioning. Native audience and full producer/callback runtime validation remain required; no whole-slice acceptance is implied.

### Native audience follow-up

Independent actual-function/SQLite review of `27564546de` confirms affected-user match/read succeeds, peer-user match/read fails, peer target override is forbidden, and missing audience user fails closed. No reachable peer-agent disclosure was demonstrated: agent creation verifies task/workspace/session ownership and forces the caller's target, while reads/ack receive verified user context. The helper's permissive non-human-owner branch must remain paired with those outer checks.

Required remaining evidence: exercise automatic materialization/delivery/read/ack using real agent-created subscriptions for two users (the new human-owned wake fixture is rejected by the real platform surface); preserve C2's audience check in A3's moved final delivery guard; explicitly test the intended upgrade behavior where DO047 legacy null audience project IDs deny old credential events. This is bounded function review, not composed workerd acceptance.

## Parent re-review of C2 `fc4fd1019`

This checkpoint remains unintegrated and CHANGES REQUIRED. Its four new lifecycle tests meaningfully exercise actual Stop/Suspend waiting for an already-enqueued HTTP report before idle, and logging exhausted delivery failure. Suspend also stops the prompt activity reporter. The fixtures enqueue directly into hosts without an attached ACP process.

Independent Go inspection confirms the reporter/enqueue/coalescing implementation is unchanged from `27564546de`: chronological transition loss, late callback admission after shutdown, and successful drain after overflow eviction remain. The new tests do not exercise actual ACP attachment, delayed callback ingress, queue overflow, or replacement during retry. Preserve the improvement while completing those three existing criteria and the SQL predecessor-CAS/global-retention/source-capture requirements above. No Go rerun was performed by this bounded reviewer; no full-slice acceptance is implied.

## Parent review of C2 `f96f67a1b`

This newer checkpoint is fetched but not integrated. Independent Cloudflare review executed its new functions against the actual D1/DO migration schemas and found three additional blockers:

- [ ] **Preserve the deployed outbox state contract.** The new `beginClaimedIntentAdmission` writes `state='admitting'` for every source, but migration 0144's CHECK excludes that value, and no subsequent migration changes it. Actual public enqueue/admit against migrations 0144/0145/0146/0148 turns an ordinary lifecycle event into `retryable_failed`, consumes attempt one, and makes zero ProjectData calls because of the CHECK error. A Drizzle type/schema edit cannot update the deployed constraint. Coordinate with B3's current claim protocol; do not introduce an uncoordinated state or rebuild the table.
- [ ] **Use authoritative per-window supersession.** `findNewerCredentialLimitEvent` applies the history limit before filtering `windowType` in JavaScript. An existing five-hour event observed at time 200, followed by five more recently received weekly events, is missed for an incoming five-hour event observed at 100 when the limit is 5; limit 6 finds it. Bounded historical truncation is not a valid latest-observation guard.
- [ ] **Bound the new supersession lookup's actual work.** Against actual DO indexes with result limit 5, the exact query costs 45,152 versus 900,152 SQLite instructions for 1,000 versus 20,000 recorded rows with tied timestamps. With conflicted history and zero returned rows, work is 7,021 versus 140,021 instructions. The subject index plus a temporary sort does not bound the scan. Use indexed durable latest authority per credential/window and test both other-window interference and large ineligible history.

B3's continued work is now preserved in `88f54aa72`: typed credential insertion, canonical payload/replay/read helpers and a supersede helper that respects live claims. Compose C2 with that current API and its later repairs, rather than replacing the source module with C2's older copy. Parent has not accepted either checkpoint. The independent probes substitute external admission and equivalent row mapping; no workerd or live provisioning is claimed.

## Parent Go re-review of `71f9054ca`

Verified by independent source review: the previous three-observation ordering case is repaired by moving replacements to the tail; utilization/amounts now participate in equivalence; Stop/Suspend close enqueue before drain; overflow makes the drain fail honestly. The new tests cover those scenarios.

Two remaining criteria are not yet met: a blocked reporter still collapses `rejected@1 → allowed@2 → rejected@3 → allowed@4` to the last pair without reporting overflow, losing a full meaningful cycle; and a callback already preparing an observation before shutdown can enqueue only after admission closes, so it is dropped after a successful drain. Coalesce only equivalent consecutive observations and add an active-callback barrier: close new admission, let admitted callbacks enqueue within the bounded drain, then report the outcome. Pause a real `SessionUpdate` using its injected clock to discriminate the race. The added clients are still manually constructed, so actual ACP attachment and credential replacement during retry remain unproven. No Go rerun is claimed by this reviewer.
