# Event source reliability and webhook review fixes

## Scope

Repair slice B of `2026-09-06-eventing-delivery-scheduling-channels.md` before accepting it into the single eventing PR. The feature commit `d2a15eee7` was integrated as `97d433a92`; the later child autosave of `.codex/config.toml` was deliberately excluded. No child PR, deployment, staging mutation, merge, main push, or additional SAM descendants. The parent owns integration and final validation.

Independent Cloudflare and security review found the defects below. Parent verification passed the shared build, 76 focused tests in six files, and 25 real Workers tests in two files. These passes do not cover the reproduced defects. Reopen all section B criteria until fixes and independent verification pass.

## Required fixes and evidence

- [ ] **Only the winning task transition emits.** `transitionTaskToTerminal` batches a guarded task update with an unconditional source-intent insertion. Executing the actual SQL with migration 0144 against a cancelled task produced task updates 0, status inserts 0, outbox inserts 1 for `task.failed`. Use a unique per-invocation proof of the winning transition to condition both status and outbox insertion atomically. Timestamp equality alone is not ownership. Prove cancellation, same-millisecond competing transitions, recovery supersession and workspace/session ownership races with a barrier between initial read and batch; the loser must persist no status or source event.
- [ ] **Fence every claim completion.** Outbox processing leases have no ownership generation. Actual SQL showed a late failure from attempt 1 clearing attempt 2's replacement lease. Return a unique claim token/generation and require it on all success, retry, permanent-failure and malformed-payload writes. Check affected rows and report the actual persisted result after losing ownership. Test late success/failure before and after replacement completion, including expired leases and lost acknowledgement.
- [ ] **Immutable identity and truthful conflicts.** Duplicate delivery inserts ignore fingerprints, and canonical `conflict` is stored as `admitted`. Actual-function tests reproduced both. Compare the persisted immutable identity and incoming envelope, retain the original retry envelope, and expose conflict as a terminal unsuccessful outcome. Avoid modifying an already-admitted original event merely because a second local enqueue reused its key incorrectly. A normal terminal hook currently rebuilds a poorer envelope than the transaction (missing `fromStatus`, workspace/session and actor metadata); have it nudge the already-persisted intent by ID instead. Test normal terminal admission as well as conflicting reuse and canonical conflict.
- [ ] **Finite indexed expiry and retention.** The recovery selector excludes expired rows forever. Pending, processing and retryable rows can remain nonterminal; all terminal rows also accumulate. The actual due query over 1,000 versus 20,000 expired pending rows used 5,045 versus 100,045 SQLite VM instructions while returning zero results. Add separately bounded indexed expiry/exhaustion terminalization, due and expired-lease candidates, and terminal-row reclamation. Avoid whole-backlog sorting and residual scans. Define a finite dedup/audit retention horizon and preserve immutable replay guarantees within it. Use a shared mutation/row budget and a configured wall budget; prove a second sweep is quiet and historical depth does not increase candidate scan work.
- [ ] **Enforce attempts at claim time.** A pending intent already at 8/8 attempts was successfully claimed as attempt 9. Put the ceiling in the atomic claim predicate, and terminalize abandoned exhausted leases. Test repeated crashes with no completion callback as well as caught failures.
- [ ] **Fresh time and bounded external admission.** Recovery reuses one sweep timestamp for every sequential claim and has no wall deadline. A slow first admission can leave later claims born expired or admit after TTL. Resolve current time for each claim/settlement, impose configured sweep and admission deadlines, and preserve fenced recoverable state after timeout or ambiguous acknowledgement. Do not suggest a JavaScript timeout cancels a remote side effect. Prove the next cron phases still run and a later sweep converges once.
- [ ] **Safe canonical webhook facts.** `generic-webhook-project-event-producer.ts` copies arbitrary body/headers into a second durable event/outbox surface with a secret-key regex that misses `clientSecret`, `privateKey`, plural `credentials`, `authToken`, and cookie/session-like fields. Memory-only actual-function probes retained the seeded canary values and canonical normalization accepted them. Use a small allowlisted fact envelope and/or the established deterministic redaction boundary; unknown free-form payload values must not silently become broadly available event metadata. Keep authenticated trigger execution's existing intended payload contract separate from the new event projection. Test realistic nested camel/snake/kebab case credentials, raw bearer/key strings and headers; canaries must be absent from event rows, outbox payloads, logs and read/delivery outputs.
- [ ] **One aggregate metadata budget.** Producer truncation is per-object/per-string but canonical admission limits total keys, total bytes and depth including wrappers. Actual producer plus canonical normalization rejects a 591-byte body with 50 keys, a 10,341-byte body, a four-level 27-byte object, and a blank JSON key. Produce metadata that meets configured canonical bounds before persisting an intent, reserve wrapper overhead, handle multibyte values and normalized-key collisions deterministically, and provide truthful truncation/outcome evidence. Valid authenticated requests must not create permanently impossible retry intents. Test through real canonical admission, not only a mocked enqueue.
- [ ] **Setup parity.** The deployment GitHub manifest adds CI/review permissions and subscriptions, but `apps/www/src/components/GitHubAppSetup.astro` still generates only `pull_requests` and `push`/`pull_request`. Align all supported app creation/setup paths and upgrade guidance. Test the actual generated public setup URL and deploy manifest for parity. This is a functional URL/config fix; preserve the visible layout and coordinate any visual evidence needed with the parent final UI sweep.
- [ ] **Native CI correlation.** `check_run` metadata omits its native `pull_requests` array; the current fixture's synthetic top-level `pull_request` does not prove real GitHub correlation. Preserve bounded native PR references along with repository, commit SHA, run/suite IDs and attempts. Use provider-shaped check-run/check-suite/workflow-run fixtures; two results for old/new commits remain distinguishable.
- [ ] **Config, structure and documentation.** Add shared defaults, validated overrides, canonical env/setup/docs mappings and bounded outcome metrics for new limits. Keep files below the repository size gate without exemptions. Append a new D1 migration if needed; do not edit migrations already integrated by siblings. No ProjectData schema changes in this slice.

## Ownership and integration

Reserve D1 migration **0148** for these fixes. B owns 0144, credential initial work 0145, credential repairs 0146, and reserved submission 0147. A owns all current ProjectData migration allocation. C2 needs the source-outbox insert boundary for an atomic conditional credential transition; coordinate the exact immutable envelope, conditional insert and claim APIs with the parent. Do not overwrite C2's files or introduce a second generic queue. Export a narrowly typed conditional capture interface where useful, without accepting arbitrary caller SQL across trust boundaries.

## Validation gate

Use real migrated D1 and canonical ProjectData admission for losing transitions, same-intent replay/conflict, claim replacement, expiry/exhaustion, retention, and admission-success/acknowledgement-loss recovery. Stubbing the external network at declared seams is appropriate; handwritten SQLite facsimiles and mocked always-created admission are not sufficient final evidence. Record measured bounded candidate/mutation work. Preserve existing webhook trigger behavior and source-label semantics. Run shared build, API lint/typecheck, relevant focused and workerd suites, migration safety, file-size checks, and setup-generation tests. Push a recoverable checkpoint before expensive checks. Complete local independent Cloudflare/security review and return exact commits, commands/results, interfaces and remaining concerns. The full parent feature and final review/staging/CI remain pending.


## Parent checkpoint review of B2 `26772e077`

The checkpoint is fetched and preserved but not integrated or accepted. These findings are from the exact committed source; they supplement the original required fixes while B2 is still implementing and testing.

- [ ] **Return the actual claim winner:** `updateClaimedIntent` correctly reloads the persisted row after its claim-token CAS loses, but `admitClaimedIntent` immediately overwrites that result with its own stale `state`, `admissionOutcome` and `eventId`. Root executed the actual admission function with a replacement-claim D1 boundary stub: persisted state remained `processing`, token `claim-b`, attempt 2, while the returned result said `admitted/created/event-a`. Preserve the reloaded winner through the public result and sweep statistics. Add the real D1 claim-replacement test through the public function, not just the settlement helper.
- [ ] **Do not exhaust an active final attempt:** The shared exhaustion phase selects every active state with `attempt_count >= max_attempts`, including `processing` with a live lease. Root executed the actual update shape against migrations 0144/0148: attempt 8 of 8 with processing lease in 2030 was immediately changed to `permanent_failed` and its claim token cleared at a 2026 sweep. A concurrent sweeper can therefore revoke a still-valid final attempt before its admission returns. Exhaust abandoned processing only after the appropriate lease/deadline, and prove active-final, abandoned-final and late-result behavior with real storage.
- [ ] **Make the new maintenance indexes usable and bound exhausted selection:** Root measured the actual query predicates/order against SQLite with migrations 0144/0148. With only healthy future pending rows, exhaustion returned zero rows but grew from 6,014 VM steps at 1,000 rows to 120,014 at 20,000; expiry grew from 5,015 to 100,015. Both plans used `idx_project_event_source_outbox_processing_lease (state=?)` plus a temporary ORDER BY tree. `state = ?` did not establish the multi-state partial-index predicate, and `attempt_count >= max_attempts` is a row-to-row comparison, not an index range. Fix the exact production queries/indexes and prove near-constant or explicitly bounded candidate work on large healthy and ineligible prefixes; zero mutated rows cannot mean zero scanned work. Include terminal retention and all due/lease paths in the same actual-plan validation.
- [ ] **Expose the credential capture boundary in coordination with C2:** The checkpoint's `ProjectEventSourceOutboxCaptureGuard` only permits `task_terminal_transition`. C2 must atomically capture credential-window state plus immutable outbox intent and use this one recovery path. Agree on a narrowly typed, server-derived winning-transition guard with C2; preserve same-batch conditional state/capture atomicity, stable payload identity and capacity behavior. Do not expose arbitrary SQL from callers or leave C2 to create an unrelated retry queue. C2 owns its credential state migration and transition semantics; B2 owns the shared outbox API.

The configured sweep row and wall budget still needs actual accounting at claim and settlement boundaries, not only maintenance row updates. Keep the original final runtime, webhook safety, manifest parity and attention-fixture requirements open until demonstrated.


Follow-up checkpoint `3bfb8a15a` was pushed/fetched during review. It removes the stale result override and replaces the partial maintenance indexes with full state-prefixed indexes. By inspection, the claim-result defect above is addressed; final integrated runtime validation remains required. Root reran the exact healthy-prefix queries against the new migration: expiry is now constant-work (17 SQLite VM instructions at both 1,000 and 20,000 rows), but exhaustion still grows from 6,013 to 120,013 instructions because its row-to-row attempt comparison scans the whole state prefix. The active-final-attempt revocation and credential capture extension remain unresolved in this checkpoint. Do not reimplement the already-removed result override; finish and test the remaining cases.
## B2 author-reported implementation notes

- Added D1 migration `0148_project_event_source_outbox_durability.sql` for `tasks.terminal_transition_id`, outbox `claim_token`, `claimed_at`, `terminalized_at`, and state-prefixed indexes for active expiry, active attempts, and terminal retention. The index definitions are full `(state, ..., id)` indexes because SQLite did not use the partial-index variant with bound state parameters.
- `transitionTaskToTerminal()` now writes a per-call `terminalTransitionId` to the winning task row and uses it as the conditional proof for both `task_status_events` and the source outbox insert. The hook receives `projectEventSourceIntentId` and only nudges the captured outbox row by ID.
- The exported producer boundary is intentionally narrow: `projectEventSourceOutboxInsertStatement(..., { capture })`, `ProjectEventSourceOutboxCaptureGuard`, `enqueueProjectEventSourceIntent`, `admitProjectEventSourceIntentById`, `enqueueAndAdmitProjectEventSourceIntent`, and `reconcileProjectEventSourceOutbox`. The current capture guard is `kind: 'task_terminal_transition'` with `projectId`, `taskId`, and `terminalTransitionId`; this leaves C2 a typed extension point without exposing arbitrary caller SQL or another queue.
- Outbox claims now generate `claim_token`, store `claimed_at`, increment attempts only when `attempt_count < max_attempts`, and settle success, retry, permanent failure, malformed payload, and canonical conflict with `WHERE id = ? AND state = 'processing' AND claim_token = ?`. Losing completions return the persisted row state instead of clearing a replacement claim.
- Immutable replay handling compares source, event type, delivery key, subject type/id, payload fingerprint, and canonical payload JSON. Conflicting active reuse becomes terminal `permanent_failed` with `admission_outcome = 'conflict'`; already-admitted originals remain unchanged.
- Reconciliation now uses separately bounded expiry/exhaustion terminalization, terminal-row retention cleanup, bounded due windows, bounded stale-processing windows, fresh per-operation time, configured sweep wall time, and configured ProjectData admission timeout. A timeout remains ambiguous and recoverable; exact replay plus claim fencing converges safely.
- Generic webhook events now persist an allowlisted fact envelope only. Body/header values and sensitive field names such as `clientSecret`, `privateKey`, `authToken`, `sessionId`, plural `credentials`, cookie/session fields, bearer strings, and raw key strings are excluded from the event/outbox projection. Authorized trigger execution still receives its original intended payload separately.
- Generic webhook metadata is canonical-normalized before intent storage using the configured ProjectData limits, with deterministic fallback to a minimal shape when aggregate bytes/keys/depth would exceed the canonical budget.
- GitHub App setup generation is aligned across deploy manifest, deploy URL generator, public Astro setup component, and static self-host wizard helper for checks/actions/issues/pull-request-review events. The public wizard script was split into a helper file to keep touched files below 800 lines while preserving the visible layout.
- Native `check_run` metadata now uses `check_run.pull_requests` and keeps bounded PR references alongside run/suite/commit identity without inventing a top-level `pull_request`. Review and review-comment bodies remain excluded from metadata.

## B2 author-reported validation notes

- Recoverable checkpoint pushed before expensive checks: `26772e077` on `sam/fix-event-source-outbox-sp4b5p`.
- Dependency/build setup: `pnpm install --frozen-lockfile`, `pnpm --filter @simple-agent-manager/shared build`, `pnpm --filter @simple-agent-manager/providers build`, and `pnpm --filter @simple-agent-manager/cloud-init build` passed.
- API static checks: `pnpm --filter @simple-agent-manager/api typecheck` passed; `pnpm --filter @simple-agent-manager/api lint` passed.
- Focused unit tests: `pnpm --filter @simple-agent-manager/api test -- tests/unit/services/project-event-source-outbox.test.ts tests/unit/services/task-terminal-transition-hooks.test.ts tests/unit/services/task-terminal-transition.test.ts tests/unit/services/github-project-event-producer.test.ts` passed, 4 files / 20 tests.
- Webhook ingress integration: `pnpm --filter @simple-agent-manager/api test -- tests/integration/webhook-trigger-ingress.test.ts` passed, 1 file / 30 tests.
- Real Workers/D1/ProjectData tests: `./node_modules/.bin/vitest run --config vitest.workers.config.ts tests/workers/project-event-source-outbox-durability.test.ts` passed, 1 file / 5 tests; `tests/workers/generic-webhook-project-events.test.ts` passed, 1 file / 2 tests; `tests/workers/github-project-events.test.ts` passed, 1 file / 8 tests.
- Setup parity: `./node_modules/.bin/vitest run --config scripts/quality/vitest.config.ts scripts/quality/github-app-setup-parity.test.ts` passed, 1 file / 2 tests.
- Migration/config checks: `pnpm quality:migration-safety`, `pnpm quality:migration-ordering`, and `pnpm quality:wrangler-bindings` passed.
- Diff hygiene: `git diff --check` passed.
- File-size evidence: B2 large touched files are under the requested 800-line cap: `project-event-source-outbox.ts` 762, `github-project-event-producer.ts` 579, `GitHubAppSetup.astro` 760, `self-host-wizard.js` 666, `self-host-wizard-helpers.js` 338. `pnpm quality:file-sizes` still fails on unrelated C2-owned `apps/api/src/services/credential-limit-events.ts` at 916 lines; this slice deliberately did not edit C2 credential telemetry files.
- Bounded work measurement with 1,000 and 20,000 old outbox rows used the exact reconciler SQL shape on local SQLite. Expiry terminalization changed 10 rows in both cases and used `idx_project_event_source_outbox_active_expiry`; terminal retention deleted 10 rows in both cases and used `idx_project_event_source_outbox_terminal_retention`; due selection inspected only the bounded inner window via `idx_project_event_source_outbox_due` and returned 0 until old expired rows are drained by bounded expiry cleanup. Measured times were 1,000 rows: expiry 1.803 ms, due 0.203 ms, terminal retention 0.270 ms; 20,000 rows: expiry 0.447 ms, due 0.094 ms, terminal retention 0.144 ms.

## B2 author-reported local review notes

These child checklist results are historical claims. Independent parent review remains CHANGES REQUIRED and governs acceptance.

- Cloudflare review: PASS. Migration 0148 is additive, does not touch ProjectData migrations or event modules, uses D1-compatible prepared statements, uses state-prefixed indexes that SQLite can use with bound state parameters, and workerd coverage exercises real migrated D1 plus canonical ProjectData admission. Wrangler changes are top-level vars only and `pnpm quality:wrangler-bindings` passed. No staging/deploy was run per task constraint.
- Security review: PASS. Generic webhook event metadata excludes raw body/header values and sensitive field names, validates canonical metadata before intent storage, and real event/read tests assert canaries are absent from outbox and ProjectData outputs while preserving the authorized trigger execution payload. GitHub review/comment body canaries are asserted absent from metadata/read outputs. The self-host helper load is same-origin static script loading.
- Env validation: PASS with fix. Added the three new optional outbox env examples to `apps/api/.env.example`; Env interface, shared defaults, Wrangler vars, and public configuration reference now agree.
- Doc sync validation: PASS. Public configuration, self-hosting guide, setup component, deploy utility, and architecture overview reflect the implemented outbox durability and GitHub App setup behavior. The parent acceptance document remains open by design.
- Constitution validation: PASS. New wall/admission/retention durations and row limits are configurable with shared defaults and Worker overrides. No new internal hardcoded URLs or deployment-specific identifiers were introduced.
- Task completion validation: PASS for B2 checklist-to-diff and criteria-to-test coverage, with one reported limitation: the global file-size quality command is blocked by the unrelated C2-owned credential telemetry file noted above.


## Final B2 integration review and B3 repair contract

B2 completed with authored `26772e077`/`3bfb8a15a`; the parent integrated them as `27834faf2`/`a991a7ccb`, preserving additive A wake configuration and both review histories. Parent independently passed shared build, 50 focused tests in five files, 15 real Worker tests in three files (53.29 seconds), and two setup-parity tests. The attention-expiry regression suite still fails eight of twelve tests because its actual SQLite fixture lacks the source-outbox schema. These results do not establish the missing race, projection or browser behavior. Independent CF/security review remains CHANGES REQUIRED.

- [ ] **Claim acquisition must retain its own token.** After the claim UPDATE, `claimIntent` loads by intent ID alone. A delayed read can return a replacement worker's token. Independent review executed the public admission function with replacement between UPDATE and read: the old invocation adopted attempt2, admitted, and cleared the replacement token. Load/return only the token this invocation acquired and stop before side effects when ownership was lost. Cover replacement before initial claim read, during admission and before/after settlement using real storage.
- [ ] **Fix both live-final-attempt exhaustion paths.** `terminalizeIneligibleClaim` and the sweep's exhaustion phase both revoke processing attempts at their ceiling before lease expiry. Preserve active ownership until its deadline, and terminalize only abandoned/exhausted work. Test duplicate admission nudges during the valid final attempt as well as sweeps, then late completion and retry exhaustion.
- [ ] **Bound exhausted selection and every mutation.** The healthy-prefix exhaustion scan remains linear even after the new full indexes. Use a seekable exhaustion predicate/state or an explicitly bounded advancing maintenance traversal. A real-function probe with sweep limit1 recorded two outbox row mutations (claim and settlement), while the budget only counted maintenance. Account all claimed/settled rows with a clearly defined finite ceiling and ensure small valid budgets can make progress. Prove all selection, expiry, exhaustion, retention and claim paths against large healthy/ineligible histories.
- [ ] **Respect remaining wall time across the external await.** The admission helper always uses the full per-intent timeout even when less sweep time remains. Actual wall5ms/admission40ms returned after about42ms; configured defaults similarly allow5s inside a2.5s sweep. Propagate the remaining wall allowance, allow only bounded checkpoint overhead, and preserve recoverable ambiguity rather than pretending the remote call was cancelled. Test continuation of later cron phases and exact eventual convergence after a late admission acknowledgement.
- [ ] **Handle upgraded terminal rows.** An existing 0144 terminal row acquires `terminalized_at=NULL` on 0148 upgrade. It never meets terminal deletion, and the real sweep leaves it forever while reporting no work. Add bounded indexed backfill/lazy maintenance or a seekable fallback timestamp. Test migration from seeded pre-0148 rows, including an empty/new install, and prove finite reclamation without a global scan.
- [ ] **Verify project identity on reserved-ID reuse.** `enqueueProjectEventSourceIntent` prefers an explicitly supplied ID, but `replayConflict` omits projectId and serialized payload excludes it. Actual function/SQLite probe requested projectq with an ID owned by projectp and received p's intent. Compare project/source/full immutable identity before returning or mutating a reused ID. Cross-project collisions must not return, admit, or alter another project's row.
- [ ] **Restore the actual self-host wizard startup.** The helper split removed `generateAppLink`, but `self-host-wizard.js` still references it during initialization. Independent review executed baseline and new full scripts in the same VM/DOM harness including the new helper loader: baseline initialized; B2 loaded both scripts then threw `ReferenceError: generateAppLink is not defined` with its started flag set. Fix the real load/init/button path, not only URL generation helpers. Add a complete-script interaction regression and browser verification of the Generate setup link flow; final parent screenshots must include this changed public surface.
- [ ] **Never copy arbitrary webhook key names as safe facts.** The actual producer and canonical normalizer accepted a body key consisting of `sk-ant-api03-` plus80characters verbatim in `metadata.request.bodyTopLevelKeys`; `session` and `passphrase` names also survive. A denylist over unknown names cannot provide the requested allowlist boundary. Drop arbitrary body/header names or project only a fixed safe vocabulary, keeping numerical shape facts. Preserve the original authorized trigger payload separately. Seed secrets as keys as well as values and assert absence through real outbox, canonical event and read/delivery outputs.
- [ ] **Make the fallback fit the configured aggregate budget.** With valid metadata maxbytes1024, ten allowed100-character header names and an empty body make both full and fallback projection throw; the fallback retains the whole header-name list. The same actual producer/normalizer input measures1549bytes and passes at2048. Validate the final fallback at configured byte/key/depth limits, removing/truncating optional facts deterministically until admissible; no unrelated500 for authenticated ingress with a valid configuration.
- [ ] **Complete C2 source capture composition.** Coordinate a narrowly typed winning credential-window transition guard and immutable outbox identity with C2. Capture conditional state and outbox intent atomically in D1, with correct capacity behavior; use the one generic autonomous recovery path. The existing task-terminal-only union is an extension point, not completed credential integration. C2 owns its state/transition/audience implementation; B3 owns this shared outbox boundary. Never accept arbitrary caller SQL.
- [ ] **Repair the integrated attention fixtures.** Run `tests/unit/durable-objects/attention-expiry.test.ts` against the integrated parent. Eight failures still log missing `project_event_source_outbox`; add the real required migration/schema fixtures, including task transition proof fields as applicable, while preserving every expected expiry/terminalization behavior. Do not mock away terminal capture or relax the assertions.

Reserve additive D1 migration0150 only if B3 needs it; D1 reserved-submission repair owns0149, C2 owns0146/DO047, and A3 owns DO046. B3 must not allocate or edit DO event migrations. Preserve existing applied migrations and the passing terminal-transition atomic capture, setup permissions/events, canonical conflict and immutable replay behavior. Keep each touched module under800 lines without exemptions.

Review evidence: CF probes used exact committed functions with SQLite/migrations0144/0148 and explicit external-admission/replacement timing substitutions. Setup failure used the complete script loader and VM/DOM harness, not a browser. Webhook projection used the actual producer, canonical normalizer and shared constants in memory; final real persisted/runtime tests remain required. Child self-checklist PASS does not supersede these independent findings.

## F0 integration and F1 mobile preview repair

F0 completed with authored commits `fc3b6f5a0` and `f3e477ece`, integrated as `ee6561186` and `d3ee5a391`. Independent full-script execution verifies initialization is repaired, personal/organization paths and ephemeral secret generation work, and MathML-shaped names remain inert text. New functional browser tests use real wizard navigation. No application security defect was found in that bounded handler review.

Parent reviewed all six uploaded desktop/mobile screenshots from `/evidence/self-host-wizard-fix/`. The desktop layout is readable, but mobile preview rows visibly overflow. Parent then reproduced this in actual Chromium with Pixel 5 emulation and CSS viewport 375 × 667: `clientWidth=375`, `visualViewport.width=375`, `innerWidth=401`, `scrollWidth=401`; the preview card ends at x355, its definition-list width is297px, its single grid track expands to361.828px, and every value ends at x400.828. Thus the existing `scrollWidth <= innerWidth` check passes while the page is 26px wider than the configured viewport. Child screenshot PNG widths1103px at DPR2.75 corroborate the expanded page. The original no-overflow claim is rejected.

- [ ] **F1:** Repair the existing mobile preview layout without redesigning the wizard. Bound grid tracks/items and wrap long URL/permission/event/name values within the preview card; do not hide overflow or clip required content. Inspect `index.astro` mobile `.sh-derived dl` / `dd` rules near1423. Check desktop and 375px/320px mobile, normal personal/org values and long/special-character data.
- [ ] **Discriminating geometry:** Assert preview value bounds against its card/list and the configured CSS viewport (`page.viewportSize().width` and/or stable client/visual viewport). Replace weak affected overflow helpers where needed. Prove the geometry test fails before the CSS repair and passes afterward; checking only visibility or comparing two equally expanded dimensions is insufficient.
- [ ] **Final browser evidence:** Re-run generation, secret and XSS suites. F0 reported82/83 XSS cases and called a mobile MathML timeout pre-existing based on desktop success; that is not proof. The checked-in selection is44 XSS cases per viewport, with some viewport-specific skips. Preserve exact passed/skipped/failed counts and classify any failure from its action/trace; compare the last working baseline only if a failure persists. Do not weaken security assertions to obtain green.
- [ ] **Screenshots:** Capture personal, org and long-value preview on desktop/mobile after the fix, inspect actual images and assert scroll/bounds. For full-page evidence reset to a deliberate scroll position so the fixed site header does not obscure arbitrary middle content. Upload recoverable evidence for the single parent PR, no child PR/staging/merge.

The parent installed Chromium and its system libraries locally to run these checks. Initial missing-browser/missing-library launches were environment failures, not application regressions. Final browser rerun is pending at this checkpoint. Keep F0's functional repair and the one-PR scope; F1 is a small adjacent correction required by actual visual review.

Parent F0 browser validation completed on integrated `d3ee5a391`: generation12 + secret12 + XSS88 = **112 passed in6.7 minutes**, one worker, no retries/skips. All mobile MathML cases passed; the child timeout did not reproduce, and no claim of a proven pre-existing failure is made. Command: from `apps/www`, `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4321 pnpm exec playwright test tests/playwright/self-host-wizard-generate-link.spec.ts tests/playwright/self-host-wizard-secrets.spec.ts tests/playwright/self-host-wizard-xss.spec.ts --workers=1 --reporter=line --max-failures=2`, against the locally built preview. This green suite does not resolve the separately measured mobile overflow; F1 must add the discriminating geometry assertion and repair it. Parent stopped its own preview after the run.

## Parent review of B3 `c2186f443`

Authored `5bb64310c` and `c2186f443` are integrated as `196a46320` and `cf636b717` for review. Parent shared build passes; six focused files pass73 tests in8.78s, including the formerly failing attention-expiry fixtures; three real Worker files pass21 tests in52.09s. These passing regressions do not close the independent budget findings below.

- [ ] **Count actual mutations:** The sweep still calls `spend(1)` after one candidate admission, even though claim and settlement each mutate a row. Independent execution of the exact final public functions against migrated SQLite produced one admitted intent and two outbox row mutations with `limit:1`. A test renamed to a work-item ceiling does not satisfy the original mutation budget. Reserve/count the required writes under an explicit finite bound; if two writes are the protocol minimum, validate/document that minimum and test useful progress at the minimum supported configuration.
- [ ] **Recompute the final time allowance:** `remainingWallMs` is captured before claim acquisition and reads, then reused when external admission starts. An actual-function probe with a20ms wall budget and35ms claim-read delay began ProjectData admission at39ms and returned at58ms. Carry an absolute deadline and recheck immediately before the RPC. Do not start a new external admission after the deadline; leave a recoverable state with bounded checkpoint overhead when time expires. Test slow D1 before the RPC, not only a stalled RPC with instantaneous D1.

Independent bounded probes verify the remaining earlier CF findings are repaired: losing the initial claim read makes zero ProjectData calls and preserves replacement ownership; a live final attempt survives nudge/sweep; the exhausted-candidate selection uses15 SQLite instructions for both1,000 and20,000 healthy rows with migration0150's expression index; legacy null terminal timestamps backfill/delete within the configured work; and foreign/same-project reserved-ID collisions preserve the original pending intent. These are executed in-memory function/SQL checks with substituted external admission and timing boundaries. Full composed credential capture and final whole-slice acceptance remain open. Source file796 lines and contract183 lines are within the implementation size ceiling at this checkpoint.

Independent security review at `c2186f443` confirms arbitrary body/header names and secret values are absent after actual canonical normalization; original input and blank source labels remain intact. A real aggregate fallback reduces1,361-byte metadata to989 bytes under a1,024-byte limit. Explicit-ID collision handling preserves the original pending row, and six indexed-payload identity mismatches are rejected. No request-reachable security blocker was demonstrated.

- [ ] **Complete the typed credential capture contract before C2 wiring:** Actual insertion accepts a winning proof for credential A while persisting an event whose subject is credential B, because project/delivery key checks omit credential subject identity. Bind credential source, subject/type and event/window identity to the proof, and test mismatch rejection. This requires an incorrect internal caller; it is a functional API integrity gap, not a demonstrated external exploit. Add a fixture that genuinely forces aggregate metadata fallback, since long included-header names now collapse to a count. Unsupported capture kinds also fall through to unconditional insertion; address this existing fail-open shape as adjacent hardening rather than describing it as a new reachable vulnerability.

## Parent validation of F1 mobile repair

F1 authored `facb4a944` and `6020fdc46`, integrated as `9be1ab478` and `0809ffaa5`. The two CSS changes constrain the mobile grid track with `minmax(0, 1fr)` and wrap long preview values. Parent independently built the public site and passed all eight new overflow scenarios on desktop/mobile in 56.6 seconds. An additional actual Chromium probe exercised personal, organization and long-value previews at 320, 375 and 1280 CSS pixels: document client/scroll widths exactly matched every configured viewport, every value remained within its list, and every value's scroll width fit its own content box. Production analytics requests were absent.

Parent opened and reviewed all six F1 screenshots in `/evidence/self-host-wizard-overflow-fix/`. Values now wrap within the cards. Parent additionally captured full-page 320px evidence after resetting scroll to the top, reviewed all three images, and uploaded them to the same private directory: personal `01M1X7SPXHW3BBVZB9KMSCVE8K`, organization `01M1X7SS3XJ225QK550FTZGXQQ`, long values `01M1X7SVBAA6G7Z8MTHNWMZ73C`. Inputs and generated secrets are disposable local test data. These images are available for the final single PR's visual evidence.

F1's task bookkeeping says completed, but its actual assistant continued processing earlier queued feedback and is strengthening the existing secrets/XSS geometry helpers; preserve that unpublished continuation and inspect its next commit. The current parent full browser regression run is still pending. F1's completion summary is evidence for its earlier commits only, not an acceptance claim for unpushed continuation.

Parent full browser rerun completed on integrated F1 CSS/test checkpoint `0809ffaa5`: **112 passed in 6.5 minutes**, one worker, no retries or skips, covering generation 12, secrets 12 and XSS 88 across desktop/mobile. The first run was interrupted with exit 143 after starting case 54 and reported no assertion failure; its cause is unknown, so it is not counted as a pass or a proven application failure. Rerun used the same selection/assertions and a terminal session with explicit 900-second process timeout; per-test timeout stayed 30 seconds. The eight added geometry tests and nine additional viewport/scenario checks passed separately. The continued F1 test-helper edits are still unpushed at this checkpoint and require their own review when available.

## Final B3 checkpoint and B4 repair contract

B3's continued work is preserved in authored `88f54aa72`, `4c2acfe61` and SAM completion autosave `deee7fa87`, integrated as `fa5feb4ec`, `e448ab7e3` and `1e7216986`. The autosave contains an indexed, limited active-capacity count plus fixture parity; it contains application changes and is intentionally retained. Task bookkeeping completed earlier, so its original structured completion evidence describes only `c2186f443`. Latest transcript/tool data, exact remote history and same-project active-task discovery were inspected before assigning the remaining repair work. No workspace was stopped, retried or deleted.

Independent exact-function/SQLite review of `deee7fa87` still reproduces:

- [ ] With sweep limit 1, one admission mutates two outbox rows (claim and settlement). Reserve/count actual writes under one finite mutation budget, including deferral/failure paths. Define a useful minimum supported configuration explicitly; do not reinterpret it as a work-item count or hide an unconditional extra write outside the budget.
- [ ] With wall budget 20ms and an initial claim-read delay of 35ms, the RPC starts at 36ms even when it resolves immediately. Carry an absolute deadline through D1 awaits and recompute immediately before external admission. Preserve a recoverable claimed/deferred outcome if time expires; do not begin new network work beyond the deadline.
- [ ] Typed credential capture rejects a wrong credential ID, but actual insertion still accepts wrong source, subject type, event type, payload window, payload observation time and unknown capture kind. Bind the typed proof to the complete source/subject/event/window identity, reject unsupported kinds, and preserve normal lifecycle transition capture. These are internal contract gaps, not a demonstrated request-reachable exploit.
- [ ] `markProjectEventSourceIntentSuperseded` scopes its UPDATE but returns an unscoped ID lookup. Calling it for project Q with an ID owned by P returns P's full intent and payload while leaving P pending. Scope the returned read to the complete supplied identity and test both no mutation and no returned foreign record. Audit the exported read helpers' intended caller authority during C2 wiring.

The credential insert branch deliberately depends on C2's migration 0146 columns. B3's standalone migrated schema lacks them, while its unit fixture creates them manually. B4 must exercise the composed real 0144/0145/0146/0148/0150 schema (and any subsequently required append-only migration) for credential capture. C2 owns credential-window tables, callbacks, latest-observation authority and audience migration 047; B4 owns the shared source-outbox contract/admission/supersession pipeline. Coordinate this boundary directly. Preserve B3's current claim-token fences, active final-attempt lease, explicit-ID collision behavior, indexed bounded maintenance, legacy terminal cleanup and canonical payload projection.

Do not copy C2's older source module or its unsupported `admitting` state: parent review of `f96f67a1b` proves migration 0144 rejects that state for every producer. Any credential-specific fence must compose with the existing canonical source protocol and immutable predecessor state, without another queue, arbitrary SQL escape hatch, history scan, or table rebuild. D1 0151 is reserved for B4 if genuinely necessary; A3 owns DO046 and C2 owns DO047. Do not edit frontend files or those DO implementations.

Validate the repaired races with actual storage/functions, then run source-outbox unit and Worker suites, lifecycle producers, webhook ingress, API lint/typecheck, migration safety/order and size checks. Keep every implementation source file under 800 lines. Push a recoverable checkpoint before heavy checks; report exact commands/results and limitations. No child PR, staging/deploy, merge, main push, or SAM grandchildren. This is the bounded B4 follow-up, not a retry or deletion of B3's workspace. The parent owns the single final PR and the full remaining feature scope.

## F1 final helper handoff

F1's continued work ended cleanly with authored `27b206522`, integrated as `52be79148`. The shared overflow helper now compares against the configured CSS viewport in all three affected specs. Parent independently passed all 12 affected desktop/mobile scenarios in 1.3 minutes, including the existing secrets and XSS visual cases. The production CSS is unchanged from the previously validated mobile fix; parent had already passed all 112 existing browser cases and nine additional geometry scenarios. Parent downloaded and opened all six replaced screenshot files under `/evidence/self-host-wizard-overflow-fix/`; the values remain contained, and the reviewed root 320px full-page evidence remains available. Child reports all 120 cases passed with no skips and a clean pushed tree. No further F1 work is outstanding for this adjacent repair; final whole-PR UI evidence remains required for future eventing surfaces.

Parent verification of integrated B3 final checkpoint completed: 75 focused tests in six files passed in 8.28s, 21 real Worker tests in three files passed in 51.96s, API typecheck and lint passed, migration safety checked 190 foreign-key relationships with zero new violations, and migration ordering passed. These regression results do not close B4's independently reproduced defects. B4 task `01M1X9RVXM759TG0NEMQRZTH3P` starts from parent `4538a0b11`; its explicit Backend Implementation profile, VM runtime, task mode and lightweight workspace were verified in SAM/D1, followed by actual model-applied/ready/ACP-start evidence at 06:45:51.984Z. Its description preserves the exact repair brief plus server-appended references. All parent validation processes and browser servers are stopped.
