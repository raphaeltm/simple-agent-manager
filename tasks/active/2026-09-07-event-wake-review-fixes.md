# Event wake and retention review fixes

## Scope and evidence

Repair slice A of `2026-09-06-eventing-delivery-scheduling-channels.md` before accepting its wake capability. Authored `c1ff1c719` is integrated as `8d4ebdb50`; the config-only autosave was excluded. Preserve the exact approved Revision 6 contract in SAM review task `01M1F086MQBD7WYTG7NB66KTA4`, including retention clarifications. This belongs to the same parent PR.

Parent shared build and 18 Workers tests pass. Reconciliation/attention has 71 passes and eight failures because its old attention fixture omits B's outbox table; B2 owns that fixture repair. The size gate fails A's new 1,037-line materialization module and expanded 879-line storage helper, plus C's separately owned 916-line service. Independent Cloudflare/security reviewers found the defects below. Their memory-only production-function and actual-migration SQLite probes are discriminating evidence, not substitutes for final workerd/runtime tests.

## Required fixes

- [ ] **Task authority and recovery lineage:** The event guard checks local batch/subscription/chat status but never owner task/runtime provenance or D1 lifecycle. The runner supplies sourceTaskGuard only for parent wakes. An actual probe with a cancelled D1 source task and active local records returned authorized, with zero D1 reads. Validate current task ownership and legitimate bounded recovery lineage before materialization, recovery and physical submission; carry the guard through the durable recovery claim. Cancellation before chat cleanup must prevent side effects. Preserve the stable chat across two recovery generations. Keep external checks outside SQLite transactions and recheck at the authoritative side-effect boundary.
- [ ] **Legacy compatibility:** Switching ownership from agent-session ID to project:chat makes existing exact visibility checks reject the unchanged live caller. Preserve bounded, explicitly versioned v1 list/get/read/ack/cancel access through verified original identity. Legacy subscriptions stay pull-only. Test upgrade, original live token, forged identity and legitimate v2 successor lineage.
- [ ] **Lease before first event:** The lease function returns null for a finite live v2 subscription without matches. Protect the actual wait from reconciliation check-ins and false terminalization until finite expiry. Keep leases outside sleep/idleness and compute-retention predicates; no-match expiry stays silent. Test check-in/attention deadlines followed by expiry and normal cleanup.
- [ ] **Due state and quiet alarms:** Materialization never reads persisted next_attempt_at, so unrelated alarms bypass backoff. Its no-work result also leaves the checkpoint behind, causing recurring minimum-delay alarms after cancellation/drain. Enforce due time, clear obsolete failure/deadline state, and calculate candidates using cooldown, lifetime, delivery count and target eligibility. Test unrelated alarms during backoff and a quiet second alarm after cancellation, exhaustion or drained work.
- [ ] **Upgrade retention:** Existing events without a scheduler row repeatedly compute now+interval while the due predicate remains false. Actual migrated-table probe: deadline86401000 was not due when reached and advanced to172801000. Persist the initial deadline lazily and run it without new admission. Test fresh/upgrade, interruption and multiple real alarm turns.
- [ ] **Actual-row budget:** Budget1 expiry updated one subscription plus1,000 matches; budget1 batch deletion cascaded through1,000 surviving retry attempts. Both reported one mutation/no remaining work. Budget match expiry separately and drain all safe eligible attempts before deleting a terminal parent, guarded against surviving attempts. Reconcile R6's cascade allowance with its actual-row ceiling explicitly. Include synthetic attempt zero and FK effects in measured writes; preserve dependency order attempts, matches, batches, events and bound maintenance bookkeeping.
- [ ] **Orphan convergence:** Repair preserves dangling non-null batch_id but deletion requires null or an existing batch, stranding the repaired row/event outside hasMore. Include old terminal matches referencing missing batches. Prove successive budget1 passes repair once, delete match, delete event, then do no work. Do not misclassify normal null-batch unmatched rows.
- [ ] **All bind budgets:** Event reads use event count+2 binds; terminalization uses match count+4. Production instrumentation produced101 binds for99 events and97 matches. Chunk by each statement's reserved count while preserving atomicity. Extend real workerd97/98/99/100/500 coverage to materialization, claims and terminalization, not only subscription expiry.
- [ ] **Fair target progress:** Oldest blocked target is selected repeatedly and returns before later ready targets. Add bounded fair candidates and per-target deferral. Test full/live-batch targetA with ready targetB and a large ineligible prefix, then A becoming eligible. Bound inspected candidates and mutations; schedule continuation only for real eligible work.
- [ ] **Failure isolation:** A caught materialization error writes its failure checkpoint without guarding that write; a second failure skips task waits, prompts and retention. Isolate both primary operation and failure persistence; retain outer-finally rearming. Inject both errors together and separately fail retention; prove later phases progress.
- [ ] **Indexed bounded scans:** Orphan ordering uses an unindexed lifecycle timestamp; match deletion orders joined rows by COALESCE. Prove every added/rewritten selector with query plans and large eligible/ineligible prefixes. Returned-row limits do not bound scans. Use seekable indexed candidates, bounded continuation and incremental accounting; daily retention must not become an empty hot loop.
- [ ] **Public contract and actual delivery:** MCP creation is v2 queued but lacks checkpoint/end-turn guidance. Return fixed truthful platform instructions; external metadata stays outside wake instructions and behind untrusted read fences. Existing worker tests manually materialize and project success. Add MCP→real shared alarm→guarded recovery→transport→read/ack coverage over two sleep/recovery cycles, plus busy targets, cancel, natural expiry/read grace, ambiguity and runtime replacement. Stub only declared external provisioning/harness transport, not behavior under test. Parent separately proves actual VM/container capability in staging.
- [ ] **Structure/config/docs:** Split both A modules below800 lines without exemptions. Preserve shared transaction acceptance and finalizer transcript-inserted hooks, unconditional mailbox notification, rollback behavior and finally rearm. Keep synthetic attempt zero separate from physical attempt ceilings. Sync defaults/barrels, Env/wrangler, API/MCP references and truthful config descriptions.

## Ownership and delivery

A2 reserves additive DO migration **046** if needed; do not rewrite045 or rebuild tables. C2 may use **047** for server-derived audience after coordination. Later channel/schedule schema IDs belong to parent. B2 owns source outbox/D1 0148 and the missing attention fixture; C2 owns credential telemetry/D1 0146; D0 owns reserved submission/D1 0147. Do not edit their implementations. Coordinate shared physical-start guards with D0; no second generic queue or placement resolver. Parent resolves shared index/barrel conflicts.

Read all original sectionA/R6 requirements as well as these defects. Validate real workerd/D1 boundaries, existing mailbox/task-wait/reconciliation/attention/access/sleep/archive behavior, migration safety, API lint/typecheck and file sizes. Build shared first; measure rows, scans and wall bounds. Push an early recoverable checkpoint before heavy checks and serialize processes. Use independent local Cloudflare/security reviewers where available; if unavailable, report that so parent review supplies it, rather than calling self-review independent.

No child PR, main push, merge, staging/deployment mutation or additional SAM descendants. Return pushed commits, exact reusable interfaces/guards, migrations/config, commands/results and limitations. Parent owns the complete feature, one green PR, final reviews and consolidated staging, and leaves it unmerged.

## Parent checkpoint review of A3 `a4c8700d6`

A3 is still implementing/validating; this pushed checkpoint is fetched but not integrated or accepted. Independent Cloudflare and security reviewers inspected the exact source and executed the bounded probes below. These are remaining code defects, not just requests to add tests. Keep every original R6 and parent acceptance criterion open until the complete behavior is demonstrated.

- [ ] **Synthetic retry rows still pin terminal batches:** Attempt retention excludes `state='retry'`, while batch deletion now requires no surviving attempts. Actual SQLite with a safe terminal batch and its synthetic mailbox-queue retry attempt changed zero rows in both phases and reported `hasMore=false`. Retire safe synthetic and terminal attempts under the same actual-row budget before parent deletion; preserve active physical attempts and their finite ceilings. Prove budget1 convergence and quiet state with the real synthetic attempt shape.
- [ ] **Report all remaining retention phases:** `deleteEligibleMatches` can exhaust its budget in the first subphase, probe only that subphase and skip the orphan/safe-batch remainder. The actual full retention function with limit1 deleted an old null-batch match, then returned `hasMore=false` while an eligible old expired match with a missing nonnull batch remained. Aggregate bounded continuation evidence across every subphase even when mutation budget is exhausted; prove repeated budget1 calls reach event deletion and quiet state.
- [ ] **Do not let one target's backoff suppress later targets:** The real wrapper invokes `run` once per subscription. After blocked A persists the project-wide next-attempt deadline, ready B's invocation returns `not_due`. Executing the actual candidate/run functions in wrapper order produced candidates[A,B] and results[A:capacity_deferred,B:not_due] with checkpoint61000. Candidate fanout alone does not establish fairness. Separate project sweep admission from per-target deferral, preserve persisted backoff on unrelated alarms, and prove automatic ready-target progress behind a blocked target.
- [ ] **Use the correct project mailbox capacity:** The new preflight counts messages for one target, although the cap is project-wide. Its actual function reported no capacity exhaustion with configured project cap5 already occupied entirely by another session. Enqueue also receives the transcript-message cap rather than the mailbox pending-project cap. Reuse the canonical mailbox capacity configuration and authoritative enforcement, with a bounded query and aggregate multi-session tests; do not create a separate capacity interpretation.
- [ ] **Remove the new hot matched-history scan:** The scheduler CTE groups matched rows during every `recalculateAlarm`. Under the actual migration indexes, SQLite VM instructions grew from17107 for1000 matched rows to340107 for20000 and used a temporary GROUP BY tree. A limited result is not bounded candidate work. Use durable indexed scheduler/candidate state and demonstrate bounded alarm scheduling under large matched and ineligible histories. Include scans in the cost evidence, not just returned/mutated rows.
- [ ] **Preserve the actual legacy fallback identity:** Pre-upgrade agent ownership was `agentSessionId ?? (taskId + ':' + sessionId)`. The new legacyOwners list uses agentSessionId and taskId, so legacy callers without an agent-session ID still lose list/get/read/ack/cancel access. Derive and verify the exact original owner identity with bounded v1-only access; never grant legacy wake behavior. Test the missing-agent-session token path, not only normal modern tokens.
- [ ] **Recheck event authority after asynchronous guards:** The final event guard checks local cancellation/expiry, then awaits D1 source authority and returns without rechecking local state. A cancellation/expiry during that await can pass. Durable recovery claims and subsequent physical allocation still carry only task/project/chat identity, without the event subscription/batch authority needed for later revocation checks. Preserve a durable event-specific guard through recovery and physical delivery/start, rechecking at the relevant authoritative boundaries. Coordinate the common TaskRunner interface with D1 repair owner; a generic task check alone does not establish event authority.
- [ ] **Make the second stable-chat recovery converge atomically:** Security review executed the actual recovery handoff SQL and source guard using SQLite. Seed original T0 cancelled/superseded_by=T1, live awaiting-followup T1 bound to chat C and recovering from T0; source guard initially passes. On T2 creation, handoff detaches T1, then final binding updates0 because T0 still points to T1 whose chat is now null. The helper throws after leaving T0/T1/T2 all unbound. Resolve and fence the legitimate current lineage member before handoff, with atomic no-loss binding on competing updates. Prove two full MCP/alarm/recovery/read-ack cycles using the real helper and task/session records, not a hand-applied success result.

The currently assigned normal-start repair owner is D1 task `01M1X0NWXR6D0PHAHCK6NZFA49` on `sam/repair-reserved-task-submission-nzfa49`; A3 retains event-specific guard and DO046 ownership. C2 owns authoritative credential audience and DO047. Preserve these boundaries while coordinating common interfaces. Do not rename a full-history scan as bounded or treat a self-checklist as independent acceptance.

Review evidence distinctions: the late-cancellation guard and legacy identity construction were executed as extracted production functions with controlled boundaries; second-recovery handoff and retention/query-cost cases used actual committed SQL in SQLite. Event revocation during durable claim/physical start was traced by code inspection without allocating compute. The fairness driver used actual selector/run functions in the wrapper's order while substituting target/capacity boundaries. No full workerd or staging approval is implied. The finite unmatched-subscription lease, MCP checkpoint/end-turn guidance, chunked bind handling, upgrade deadline and secondary-error isolation appear improved by inspection; preserve those fixes while addressing the remaining cases.

Additional A3 read-grace regression (code inspection, not executed): `project-events-wake-targets.ts:38-57` takes the minimum of delivery expiry and readable-until. A delivered but unacknowledged batch whose delivery TTL has elapsed therefore stops occupying its target while its read grace is still live. The earlier helper used readable-until for this state. Preserve target occupancy through the applicable delivered/unacked read grace; add a case with delivery TTL shorter than read grace and verify the next wake remains deferred until acknowledgement or grace expiry.

## Parent re-review of A3 `aaeb7a7f6`

This checkpoint remains unintegrated and CHANGES REQUIRED. Independent reviewers reran the actual committed functions and migration SQL. Added tests do not establish the outstanding behavior below.

- **Verified repair:** Retention now reports continuation across null/missing-batch match subphases. With limit 1, four passes delete both matches and both events, followed by a quiet fifth pass.
- **Still reproduced:** A synthetic retry-0 attempt pins an acknowledged batch (both deletion phases mutate 0 and report no continuation); project mailbox cap 5 occupied by another session still reports target capacity available; an unacknowledged delivery with TTL 1000 and readable-until 10000 loses its occupancy lease at time 2000; wrapper order produces blocked A `capacity_deferred` then ready B `not_due`, with project checkpoint 61000. Scheduler matched-history grouping uses 17,105 SQLite instructions for 1,000 rows and 340,105 for 20,000, including a temporary GROUP BY tree.
- **Still reproduced:** Cancellation during the final D1 authorization await returns successful authority (`null` failure), with one local check; checking local state immediately afterward returns `failed`. Event-specific authority remains absent from recovery's persisted start guard.
- **Still reproduced:** Actual T0 → T1 → T2 handoff SQL detaches one chat binding and binds zero, leaving all three tasks unbound. Even a separately pre-bound T2 fixture has disagreeing guards: source authority accepts it, while `isSessionRecoveryTaskAuthorized` rejects queued/waking T2 because it requires the root task to remain nonterminal. The new test pre-seeds T2 instead of executing handoff.
- **Still reproduced:** Verified caller resolution without an agent-session ID yields legacy owner `T`, and rejects the historical `T:C` identity. Preserve the exact pre-upgrade fallback for v1 access only.

The new server-derived `ownerTaskId` and MCP override rejection are useful, but do not repair these paths. C2 audience checks must extend A3's current helper bodies. Two actual MCP/alarm/recovery/read-ack cycles remain required. These probes substitute external boundaries and do not allocate compute or constitute staging evidence.

## Parent re-review of A3 `963d61f9c`

Independent actual-function/SQLite probes verify that the simple synthetic retry-zero case now reclaims, and the actual alarm wrapper proceeds past blocked target A to ready B while later invocations retain persisted backoff. The earlier cross-phase retention continuation repair remains intact.

The real checkpoint callback sequence `retry → accepted → acknowledged` still pins terminal history: retention deletes the accepted attempt but leaves completed physical retry attempt one (`completed_at=100`), synthetic retry zero and the acknowledged batch. Five limit-one passes report no continuation. The new retention predicate treats every historical physical retry as active despite its completion timestamp. Reclaim completed retry history only when its parent is safely terminal, preserve genuinely active delivery, and test this callback sequence rather than successful first-attempt delivery alone.

The other three Cloudflare findings are unchanged and reproduced again: a project mailbox cap occupied by another session is missed; delivered/unacknowledged target occupancy ends at delivery TTL despite a longer live read grace; and scheduler matched-history grouping costs 17,105 versus 340,105 SQLite instructions for 1,000 versus 20,000 matched rows. These remain blockers.

This commit changes only alarm/materialization/retention code and related tests. It does not modify the previously reproduced security/lifecycle defects in the final post-D1 cancellation check, recovery event authority and second-generation handoff, or historical `task:chat` fallback ownership. Child-reported security approval cannot close those unchanged paths. The current source still returns directly from the asynchronous source guard after the local check. All remaining supplemental criteria must be reconciled before A3 is accepted or dependent feature validation claims same-chat recovery.

## Recovery parent review of FINAL A3 `8f1f537b4`

Replacement coordinator 01M1XCYS20J34FTHZ0AB5G5M8W recovered the original parent history at99b36c364 and merged final A3 into c07772fd5. Two fresh local independent reviewers inspected that exact integrated source and ran bounded actual-function/SQLite probes. This is CHANGES REQUIRED; A3's completion summary and reported focused test passes do not close the following executable defects. Final A3 scheduler/retention/wake-target helpers are byte-identical to the integrated versions.

- [ ] **Completed retry history:** actual retry→accepted→ack shape with physical retry1 completed_at100, synthetic retry0 and safe acknowledged parent: five budget1 passes delete only the accepted row on pass1, then stop with completed retry1/synthetic0/batch remaining. Retire completed physical retries only under safe terminal parent authority and actual global write budget; preserve genuinely in-flight retries. Prove repeated bounded passes reach quiet state.
- [ ] **Project mailbox capacity and acceptance cap:** actual `isTargetAtWakeCapacity` returns false when another chat occupies configured project cap5. The materialization options also pass the transcript message cap to mailbox acceptance. Reuse one canonical project-wide pending-mailbox configuration and enforcement; bound the preflight query and avoid broad historical scans. Test cross-chat occupancy and actual atomic rollback.
- [ ] **Read-grace occupancy:** delivered/unacked batch with delivery TTL1000 and readableUntil10000 at now2000 yields null lease. Use the correct deadline for each state; prevent a second wake during delivered/unacked read grace. Acknowledge/expiry must release occupancy without empty loops.
- [ ] **Hot scheduler scan:** actual final scheduler CTE with real migration indexes performs17105 SQLite VM instructions for1000 matches and340105 for20000, using a temporary GROUP BY. Replace per-recalculate historical grouping with durable indexed due/candidate state and bounded maintenance; no renamed aggregate or result-only LIMIT. Prove bounded work across large matched and ineligible histories, upgrade bootstrap, retries/cancel/drain and candidate fairness. Parent allocates additive DO050 if needed (E1 owns048, schedules049); never rewrite046/047.
- [ ] **Remaining public bind overflows:** actual `readMatchesByIds` and `readEventsForMatches` produce99/100/101/102/502 binds for97/98/99/100/500 input IDs. Per-statement chunk all public paths, not only the newly chunked materialization helpers. Prove public batch/read/attempt paths under real workerd100-bind ceiling while preserving ordering/atomicity and caller limits.
- [ ] **Cancellation during final D1 read:** extracted actual final delivery guard returns null after the D1 await cancels the subscription; localReads1; an immediate actual local guard returns failed. Recheck local event authority after the final await, and exercise cancellation/expiry/kill/retarget at every actual asynchronous side-effect boundary.
- [ ] **Persist event authority through recovery:** `sourceTaskGuardForClaim` emits only task/project/chat, and session-recovery passes only source task ID to TaskRunner. Carry validated event subscription/batch/delivery authority durably through recovery reservation and the authoritative physical allocation/start boundary. Fail closed for unknown/mismatched guards; do not trust a task-only check as event revocation. Compose with D1's `reserved_submission` guard, never replace it or add a parallel placement implementation.
- [ ] **Second recovery atomicity and consistent authority:** actual handoff SQL for valid T0 cancelled/superseded_byT1, live same-chat T1, newly insertedT2 detaches1/binds0, leaving all chats unbound. Separately prebinding queued/wakingT2 gives source guard=true and TaskRunner recovery guard=false. Fix atomic handoff with current legitimate lineage ownership and consistent bounded authority. Prove two actual recovery creations through the production helper, not pre-seeded final success; losing CAS must preserve prior binding. Include failure rollback, competing claim and cancellation.
- [ ] **Exact historical v1 fallback:** verified caller without agentSessionId yields legacy ownerT; historical T:C fails access. Derive the historical fallback exactly, with bounded v1-only list/get/read/ack/cancel compatibility, never legacy wake or arbitrary caller identity widening.

Pending D1 candidate a9173a095 has byte-identical session-recovery.ts and session-recovery-authority.ts and only adds `reserved_submission` start guard support. It does not close the event-specific or two-generation defects above. D1 owns its ordinary start/allocation repair; the new A4 owner owns these event/recovery changes and must coordinate narrow TaskRunner guard integration. A4 may reserve D10152 if durable guard persistence needs a D1 schema; B4 owns0151. No conflicting migration rewrite or table rebuild.

Preserve verified improvements: fair wrapper admission behind a deferred target, bind chunking in new materialization paths, orphan cross-phase continuation, fixed IDs-only wake and untrusted read fence, finite unmatched leases, due/backoff/error isolation. Full original R6/A criteria still apply. Add a real MCP→shared alarm→production recovery handoff→external transport boundary→read/ack vertical test over TWO generations, with only provisioning/harness transport substituted. Existing manual materialization/projected success tests are not this evidence. Capture exact tests and independent re-review of every listed reproduction before claiming completed; no PR, staging/deploy, merge/main push or SAM grandchildren.

## Recovery review of interrupted A4 checkpoint `2ea651fde`

A4 failed as unresponsive at09:17:26.758Z. Its pushed checkpoint `2ea651fde7bc5d9e6f36dd41330f0d7f14524d7e` is fetched but unintegrated; stopped workspace on the preserved shared node is untouched. No claim that this commit contains every unpublished delta. Independent local reviewers used exact committed production functions/SQL, bounded probes, and no checkout or host mutation.

Verified scoped repairs: post-D1 event cancellation fails with two local checks; actual v1 no-agent-session caller resolves T:C and accesses legacy records; simple T0→T1→T2 SQL now detaches2/binds1 and passes queued/waking TaskRunner guard under both predecessor insertion orders. Guard fields propagate into persisted TaskRunner configuration. Project mailbox occupancy on another target now blocks at cap5; deliveredTTL1000/readable10000 atnow2000 returns10000; physical retry1 plus synthetic0 drains over budget1 passes, quiet fifth pass; exported read/read/update helpers stay within100binds at97/98/99/100/500.

Remaining CHANGES REQUIRED:

- Container cold-wake guards in `vm-agent-container.ts`244/583 still call the D1-only source validator and ignore carried `projectEventWake`. Actual proxyHttpGuarded/live-D1-authority probe reaches substituted physical boundary once without checking event revocation. Enforce subscription/batch authority at final in-container guards.
- TaskRunner `hasRecoveryAuthority`455 reads D1 then returns the final event-authority RPC465. Source cancellation during that RPC still returns true with oneD1read. Recheck task/claim authority after this await.
- NodeLifecycle warm-claim SQL109 rejects legitimate queued/waking T2 owningC with cancelled originalT0/superseded_byT2: updates0 while TaskRunner guard returns true. Use consistent bounded successor semantics at this authoritative claim boundary.
- New indexed wake due state is not maintained on all match mutations. `updateMatchesForBatch` claims the lastmatch without refreshing wake_due_at. Three actual materializer/scheduler calls returnno_due_work whilewake_due_at1 andnextalarmnow+minDelay. Public pull claiming similarly lacks refresh. Maintain due state on every mutation path and clear/repair stale due state on the empty-candidate backstop.
- Overall materialization scan work is still unbounded. `terminalizeIneligibleWakeMatches`'s target-no-longer-active selector withLIMIT1 and oneactive target returns0 after33,024SQLiteinstructions for1k unmatchedmatches versus660,024for20k. Actual relevant migrationindexes and reducedSQLitefixture were used, notfullworkerd. Due-state scheduling alone does not remove this per-entry scan.

Snapshot recovery claim persistence still lacks event-specific checks before later TaskRunner validation; cover cancellation across that interval. No full two-generation MCP→sharedalarm→actualrecovery→externaltransport→read/ack proof exists. Newhandoff test mocksTaskRunner; worker manuallymaterializes andchangesstate. Retain original acceptance contract and distinguish these gaps from D1 ordinaryreservedsubmission findings.

CF review limitations: initial probes used actual migration schemas with unrelated fixture CHECK constraints bypassed; empty-alarm fixture also disabled FKs. The scan fixture used reduced tables and relevant actual indexes. Orphan retention's unindexed lifecycle_checked_at ordering remains an additional acceptance gap, not repaired byA4. No fresh full runtime/actual-write-ceiling/fairness/quiet-alarm suite was run for this interrupted checkpoint.
