# Event wake and retention review fixes

## Scope and evidence

Repair slice A of `2026-09-06-eventing-delivery-scheduling-channels.md` before accepting its wake capability. Authored `c1ff1c719` is integrated as `8d4ebdb50`; the config-only autosave was excluded. Preserve the exact approved Revision 6 contract in SAM review task `01M1F086MQBD7WYTG7NB66KTA4`, including retention clarifications. This belongs to the same parent PR.

Parent shared build and 18 Workers tests pass. Reconciliation/attention has 71 passes and eight failures because its old attention fixture omits B's outbox table; B2 owns that fixture repair. The size gate fails A's new 1,037-line materialization module and expanded 879-line storage helper, plus C's separately owned 916-line service. Independent Cloudflare/security reviewers found the defects below. Their memory-only production-function and actual-migration SQLite probes are discriminating evidence, not substitutes for final workerd/runtime tests.

## Required fixes

- [ ] **Task authority and recovery lineage:** The event guard checks local batch/subscription/chat status but never owner task/runtime provenance or D1 lifecycle. The runner supplies sourceTaskGuard only for parent wakes. An actual probe with a cancelled D1 source task and active local records returned authorized, with zero D1 reads. Validate current task ownership and legitimate bounded recovery lineage before materialization, recovery and physical submission; carry the guard through the durable recovery claim. Cancellation before chat cleanup must prevent side effects. Preserve the stable chat across two recovery generations. Keep external checks outside SQLite transactions and recheck at the authoritative side-effect boundary.
- [ ] **Legacy compatibility:** Switching ownership from agent-session ID to project:chat makes existing exact visibility checks reject the unchanged live caller. Preserve bounded, explicitly versioned v1 list/get/read/ack/cancel access through verified original identity. Legacy subscriptions stay pull-only. Test upgrade, original live token, forged identity and legitimate v2 successor lineage.
- [ ] **Lease before first event:** The lease function returns null for a finite live v2 subscription without matches. Protect the actual wait from reconciliation check-ins and false terminalization until finite expiry. Keep leases outside sleep/idleness and compute-retention predicates; no-match expiry stays silent. Test check-in/attention deadlines followed by expiry and normal cleanup.
- [ ] **Due state and quiet alarms:** Materialization never reads persisted next_attempt_at, so unrelated alarms bypass backoff. Its no-work result also leaves the checkpoint behind, causing recurring minimum-delay alarms after cancellation/drain. Enforce due time, clear obsolete failure/deadline state, and calculate candidates using cooldown, lifetime, delivery count and target eligibility. Test unrelated alarms during backoff and a quiet second alarm after cancellation, exhaustion or drained work.
- [ ] **Upgrade retention:** Existing events without a scheduler row repeatedly compute now+interval while the due predicate remains false. Actual migrated-table probe: deadline86401000 was not due when reached and advanced to172801000. Persist the initial deadline lazily and run it without new admission. Test fresh/upgrade, interruption and multiple real alarm turns.
- [ ] **Actual-row budget:** Budget1 expiry updated one subscription plus1,000 matches; budget1 batch deletion cascaded through1,000 surviving retry attempts. Both reported one mutation/no remaining work. Budget match expiry separately and drain all safe eligible attempts before deleting a terminal parent, guarded against surviving attempts. Reconcile R6's cascade allowance with its actual-row ceiling explicitly. Include synthetic attempt zero and FK effects in measured writes; preserve dependency order attempts, matches, batches, events and bound maintenance bookkeeping.
- [ ] **Orphan convergence:** Repair preserves dangling non-null batch_id but deletion requires null or an existing batch, stranding the repaired row/event outside hasMore. Include old terminal matches referencing missing batches. Prove successive budget1 passes repair once, delete match, delete event, then do no work. Do not misclassify normal null-batch unmatched rows.
- [ ] **All bind budgets:** Event reads use event count+2 binds; terminalization uses match count+4. Production instrumentation produced101 binds for99 events and97 matches. Chunk by each statement's reserved count while preserving atomicity. Extend real workerd97/98/99/100/500 coverage to materialization, claims and terminalization, not only subscription expiry.
- [ ] **Fair target progress:** Oldest blocked target is selected repeatedly and returns before later ready targets. Add bounded fair candidates and per-target deferral. Test full/live-batch targetA with ready targetB and a large ineligible prefix, then A becoming eligible. Bound inspected candidates and mutations; schedule continuation only for real eligible work.
- [ ] **Failure isolation:** A caught materialization error writes its failure checkpoint without guarding that write; a second failure skips task waits, prompts and retention. Isolate both primary operation and failure persistence; retain outer-finally rearming. Inject both errors together and separately fail retention; prove later phases progress.
- [ ] **Indexed bounded scans:** Orphan ordering uses an unindexed lifecycle timestamp; match deletion orders joined rows by COALESCE. Prove every added/rewritten selector with query plans and large eligible/ineligible prefixes. Returned-row limits do not bound scans. Use seekable indexed candidates, bounded continuation and incremental accounting; daily retention must not become an empty hot loop.
- [ ] **Public contract and actual delivery:** MCP creation is v2 queued but lacks checkpoint/end-turn guidance. Return fixed truthful platform instructions; external metadata stays outside wake instructions and behind untrusted read fences. Existing worker tests manually materialize and project success. Add MCP→real shared alarm→guarded recovery→transport→read/ack coverage over two sleep/recovery cycles, plus busy targets, cancel, natural expiry/read grace, ambiguity and runtime replacement. Stub only declared external provisioning/harness transport, not behavior under test. Parent separately proves actual VM/container capability in staging.
- [ ] **Structure/config/docs:** Split both A modules below800 lines without exemptions. Preserve shared transaction acceptance and finalizer transcript-inserted hooks, unconditional mailbox notification, rollback behavior and finally rearm. Keep synthetic attempt zero separate from physical attempt ceilings. Sync defaults/barrels, Env/wrangler, API/MCP references and truthful config descriptions.

## Ownership and delivery

A2 reserves additive DO migration **046** if needed; do not rewrite045 or rebuild tables. C2 may use **047** for server-derived audience after coordination. Later channel/schedule schema IDs belong to parent. B2 owns source outbox/D1 0148 and the missing attention fixture; C2 owns credential telemetry/D1 0146; D0 owns reserved submission/D1 0147. Do not edit their implementations. Coordinate shared physical-start guards with D0; no second generic queue or placement resolver. Parent resolves shared index/barrel conflicts.

Read all original sectionA/R6 requirements as well as these defects. Validate real workerd/D1 boundaries, existing mailbox/task-wait/reconciliation/attention/access/sleep/archive behavior, migration safety, API lint/typecheck and file sizes. Build shared first; measure rows, scans and wall bounds. Push an early recoverable checkpoint before heavy checks and serialize processes. Use independent local Cloudflare/security reviewers where available; if unavailable, report that so parent review supplies it, rather than calling self-review independent.

No child PR, main push, merge, staging/deployment mutation or additional SAM descendants. Return pushed commits, exact reusable interfaces/guards, migrations/config, commands/results and limitations. Parent owns the complete feature, one green PR, final reviews and consolidated staging, and leaves it unmerged.
